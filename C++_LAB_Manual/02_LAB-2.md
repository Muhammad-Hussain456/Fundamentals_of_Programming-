# LAB 2: Fundamentals of C++ - Final Complete Version

## Objectives:
* **Understand variables, constants, data types, and type conversions**
* **Use input/output operations effectively**

---

## Exercises (15 Tasks)

### 1. Program to Declare and Display Different Data Types

**Algorithm:**
1. Start
2. Declare variables of different data types:
   - integerVar (int) = 10
   - floatVar (float) = 3.14
   - charVar (char) = 'A'
   - doubleVar (double) = 3.1415926535
   - stringVar (string) = "Hello, C++!"
3. Display each variable with its type
4. End

**Program:**
```cpp
#include <iostream>
#include <string>
using namespace std;

int main() {
    int integerVar = 10;
    float floatVar = 3.14;
    char charVar = 'A';
    double doubleVar = 3.1415926535;
    string stringVar = "Hello, C++!";
    
    cout << "Integer: " << integerVar << endl;
    cout << "Float: " << floatVar << endl;
    cout << "Character: " << charVar << endl;
    cout << "Double: " << doubleVar << endl;
    cout << "String: " << stringVar << endl;

    return 0;
}
```

---

### 2. Demonstrate Implicit and Explicit Type Conversions

**Algorithm:**
1. Start
2. Demonstrate implicit conversion:
   - Assign int to float (automatic conversion)
3. Demonstrate explicit conversion:
   - Cast float to int using (int)
   - Cast int to float using (float)
4. Display results of all conversions
5. End

**Program:**
```cpp
#include <iostream>
using namespace std;

int main() {
    // Implicit Conversion
    int intVar = 5;
    float floatVar = intVar;  // Implicit conversion from int to float
    cout << "Implicit conversion (int to float): " << intVar << " -> " << floatVar << endl;
    
    // Explicit Conversion
    float num = 3.7;
    int result1 = (int)num;  // Explicit conversion from float to int
    float result2 = (float)intVar;  // Explicit conversion from int to float
    
    cout << "Explicit conversion (float to int): " << num << " -> " << result1 << endl;
    cout << "Explicit conversion (int to float): " << intVar << " -> " << result2 << endl;

    return 0;
}
```

---

### 3. Program to Compute Simple Interest

**Algorithm:**
1. Start
2. Read principal amount (P)
3. Read rate of interest (R)
4. Read time in years (T)
5. Calculate simple interest: SI = (P × R × T) / 100
6. Display simple interest
7. End

**Program:**
```cpp
#include <iostream>
using namespace std;

int main() {
    float principal, rate, time, interest;
    
    cout << "Enter principal amount: ";
    cin >> principal;
    cout << "Enter rate of interest: ";
    cin >> rate;
    cout << "Enter time in years: ";
    cin >> time;

    interest = (principal * rate * time) / 100;
    cout << "Simple Interest: " << interest << endl;

    return 0;
}
```

---

### 4. Calculate Compound Interest

**Algorithm:**
1. Start
2. Read principal amount (P)
3. Read rate of interest (R)
4. Read time in years (T)
5. Calculate amount: A = P × (1 + R/100)^T
6. Calculate compound interest: CI = A - P
7. Display compound interest
8. End

**Program:**
```cpp
#include <iostream>
#include <cmath>
using namespace std;

int main() {
    float principal, rate, time, amount, interest;
    
    cout << "Enter principal amount: ";
    cin >> principal;
    cout << "Enter rate of interest: ";
    cin >> rate;
    cout << "Enter time in years: ";
    cin >> time;

    amount = principal * pow((1 + rate / 100), time);
    interest = amount - principal;

    cout << "Compound Interest: " << interest << endl;
    cout << "Total Amount: " << amount << endl;

    return 0;
}
```

---

### 5. Convert Total Seconds into Hours, Minutes, and Seconds

**Algorithm:**
1. Start
2. Read total seconds
3. Calculate hours = totalSeconds / 3600
4. Calculate remaining seconds = totalSeconds % 3600
5. Calculate minutes = remaining seconds / 60
6. Calculate seconds = remaining seconds % 60
7. Display hours, minutes, seconds
8. End

**Program:**
```cpp
#include <iostream>
using namespace std;

int main() {
    int totalSeconds, hours, minutes, seconds;
    
    cout << "Enter total seconds: ";
    cin >> totalSeconds;

    hours = totalSeconds / 3600;
    int remainingSeconds = totalSeconds % 3600;
    minutes = remainingSeconds / 60;
    seconds = remainingSeconds % 60;

    cout << "Time: " << hours << " hours, " << minutes << " minutes, " << seconds << " seconds" << endl;

    return 0;
}
```

---

### 6. Compute Area and Perimeter of a Rectangle

**Algorithm:**
1. Start
2. Read length of rectangle
3. Read width of rectangle
4. Calculate area = length × width
5. Calculate perimeter = 2 × (length + width)
6. Display area and perimeter
7. End

**Program:**
```cpp
#include <iostream>
using namespace std;

int main() {
    float length, width, area, perimeter;
    
    cout << "Enter length of rectangle: ";
    cin >> length;
    cout << "Enter width of rectangle: ";
    cin >> width;

    area = length * width;
    perimeter = 2 * (length + width);

    cout << "Area: " << area << endl;
    cout << "Perimeter: " << perimeter << endl;

    return 0;
}
```

---

### 7. Convert Fahrenheit to Celsius

**Algorithm:**
1. Start
2. Read temperature in Fahrenheit
3. Calculate Celsius = (Fahrenheit - 32) × 5/9
4. Display temperature in Celsius
5. End

**Program:**
```cpp
#include <iostream>
using namespace std;

int main() {
    float fahrenheit, celsius;
    
    cout << "Enter temperature in Fahrenheit: ";
    cin >> fahrenheit;

    celsius = (fahrenheit - 32) * 5 / 9;
    cout << "Temperature in Celsius: " << celsius << endl;

    return 0;
}
```

---

### 8. Calculate BMI (Body Mass Index)

**Algorithm:**
1. Start
2. Read weight in kilograms
3. Read height in meters
4. Calculate BMI = weight / (height × height)
5. Display BMI
6. End

**Program:**
```cpp
#include <iostream>
using namespace std;

int main() {
    float weight, height, bmi;
    
    cout << "Enter weight (kg): ";
    cin >> weight;
    cout << "Enter height (meters): ";
    cin >> height;

    bmi = weight / (height * height);
    cout << "Your BMI is: " << bmi << endl;

    return 0;
}
```

---

### 9. Display ASCII Value of Any Character

**Algorithm:**
1. Start
2. Read a character
3. Convert character to its ASCII value using int()
4. Display ASCII value
5. End

**Program:**
```cpp
#include <iostream>
using namespace std;

int main() {
    char ch;
    
    cout << "Enter a character: ";
    cin >> ch;

    cout << "The ASCII value of '" << ch << "' is " << int(ch) << endl;

    return 0;
}
```

---

### 10. Compute Gross and Net Salary

**Algorithm:**
1. Start
2. Read basic salary
3. Read allowances
4. Read deductions
5. Calculate gross salary = basic salary + allowances
6. Calculate net salary = gross salary - deductions
7. Display gross and net salary
8. End

**Program:**
```cpp
#include <iostream>
using namespace std;

int main() {
    float basicSalary, allowances, deductions, grossSalary, netSalary;
    
    cout << "Enter basic salary: ";
    cin >> basicSalary;
    cout << "Enter allowances: ";
    cin >> allowances;
    cout << "Enter deductions: ";
    cin >> deductions;

    grossSalary = basicSalary + allowances;
    netSalary = grossSalary - deductions;

    cout << "Gross Salary: " << grossSalary << endl;
    cout << "Net Salary: " << netSalary << endl;

    return 0;
}
```

---

### 11. Student Report Form

**Algorithm:**
1. Start
2. Read student name
3. Read roll number
4. Read marks
5. Display formatted report with borders
6. End

**Program:**
```cpp
#include <iostream>
#include <string>
using namespace std;

int main() {
    string name;
    int rollNo;
    float marks;
    
    cout << "Enter student name: ";
    getline(cin, name);
    cout << "Enter roll number: ";
    cin >> rollNo;
    cout << "Enter marks: ";
    cin >> marks;

    cout << "\n---------------------------" << endl;
    cout << "      STUDENT REPORT" << endl;
    cout << "---------------------------" << endl;
    cout << "Name: " << name << endl;
    cout << "Roll No: " << rollNo << endl;
    cout << "Marks: " << marks << endl;
    cout << "---------------------------" << endl;

    return 0;
}
```

---

### 12. Demonstrate Use of Constants

**Algorithm:**
1. Start
2. Declare constants using const keyword:
   - PI = 3.14159
   - MAX_USERS = 100
   - TAX_RATE = 0.15
3. Display all constant values
4. End

**Program:**
```cpp
#include <iostream>
using namespace std;

int main() {
    const float PI = 3.14159;
    const int MAX_USERS = 100;
    const float TAX_RATE = 0.15;
    
    cout << "Value of PI: " << PI << endl;
    cout << "Maximum number of users: " << MAX_USERS << endl;
    cout << "Tax rate: " << TAX_RATE << endl;

    return 0;
}
```

---

### 13. Compute Volume of a Cylinder

**Algorithm:**
1. Start
2. Read radius of cylinder
3. Read height of cylinder
4. Calculate volume = π × radius² × height
5. Display volume
6. End

**Program:**
```cpp
#include <iostream>
using namespace std;

int main() {
    const float PI = 3.14159;
    float radius, height, volume;
    
    cout << "Enter radius of cylinder: ";
    cin >> radius;
    cout << "Enter height of cylinder: ";
    cin >> height;

    volume = PI * radius * radius * height;
    cout << "Volume of the cylinder: " << volume << endl;

    return 0;
}
```

---

### 14. Convert Days into Years, Weeks, and Days

**Algorithm:**
1. Start
2. Read total days
3. Calculate years = totalDays / 365
4. Calculate remaining days after years = totalDays % 365
5. Calculate weeks = remaining days / 7
6. Calculate days = remaining days % 7
7. Display years, weeks, days
8. End

**Program:**
```cpp
#include <iostream>
using namespace std;

int main() {
    int totalDays, years, weeks, days;
    
    cout << "Enter total number of days: ";
    cin >> totalDays;

    years = totalDays / 365;
    int remainingDays = totalDays % 365;
    weeks = remainingDays / 7;
    days = remainingDays % 7;

    cout << "Years: " << years << endl;
    cout << "Weeks: " << weeks << endl;
    cout << "Days: " << days << endl;

    return 0;
}
```

---

### 15. Compute Area of a Circle

**Algorithm:**
1. Start
2. Read radius of circle
3. Calculate area = π × radius²
4. Display area
5. End

**Program:**
```cpp
#include <iostream>
using namespace std;

int main() {
    const float PI = 3.14159;
    float radius, area;
    
    cout << "Enter radius of the circle: ";
    cin >> radius;

    area = PI * radius * radius;
    cout << "Area of the circle: " << area << endl;

    return 0;
}
```

---

## Key Concepts Covered:

**Data Types:**
- Fundamental types: int, float, double, char, bool
- Derived types: string (from C++ library)

**Type Conversions:**
- Implicit conversion (automatic)
- Explicit conversion (manual casting)

**Constants:**
- Using `const` keyword
- Naming conventions for constants

**Input/Output Operations:**
- `cin` for input
- `cout` for output
- Formatted output

**Mathematical Calculations:**
- Basic arithmetic operations
- Using mathematical formulas
- Order of operations

**Real-world Applications:**
- Financial calculations (interest)
- Geometry calculations
- Unit conversions
- BMI calculation
- Salary computations

This lab provides comprehensive practice with fundamental C++ concepts including variables, data types, type conversions, constants, and input/output operations through practical, real-world problems.
